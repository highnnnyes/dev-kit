# Development Constitution (dev/CLAUDE.md)

이 파일은 dev/ 하위 모든 프로젝트에 상속되는 공통 헌법이다.
프로젝트별 세부 규칙(스택, 진행상황, 오버라이드)은 각 프로젝트의 CLAUDE.md에 둔다.
하위 CLAUDE.md가 이 파일과 충돌하면 하위가 우선한다. 오버라이드는 명시적으로 적는다.

규율 표기: **[엄격]** = 정확히 따른다, 맥락·급함을 이유로 완화하지 않는다.
**[유연]** = 원칙을 유지하되 적용 방식은 맥락에 맞게 적응한다.

---

## 1. Core Principles (Karpathy)

### Think Before Coding
- 가정하지 말 것. 불확실하면 추측 대신 질문한다.
- 해석이 여러 개면 몰래 하나를 고르지 말고 선택지를 제시한다.
- 더 단순한 방법이 있으면 반박한다.
- 혼란스러우면 멈추고 무엇이 불명확한지 명시한다 (Confusion Protocol).
- 아키텍처 결정이 필요한 지점에서는 절대 임의로 진행하지 않는다.

### Simplicity First
- 요청된 것만 만든다. 요청 밖 기능·유연성·설정성 금지.
- 1회용 코드에 추상화 금지. 발생 불가능한 시나리오에 에러 처리 금지.
- 200줄이 50줄로 가능하면 다시 쓴다.
- 테스트: "시니어 엔지니어가 보면 과하다고 할까?" → Yes면 단순화.

### Surgical Changes
- 변경된 모든 줄은 사용자 요청으로 직접 소급 가능해야 한다.
- 인접 코드·주석·포맷을 "개선"하지 않는다. 안 깨진 것을 리팩토링하지 않는다.
- 기존 스타일을 따른다. 내 취향과 달라도.
- 무관한 데드코드 발견 시: 언급만 하고 삭제하지 않는다.
- 내 변경으로 생긴 고아(미사용 import/변수/함수)만 정리한다.

### Goal-Driven Execution
- 명령형 작업을 검증 가능한 목표로 변환한다:
  - "검증 추가해" → "잘못된 입력 테스트를 먼저 쓰고 통과시켜라"
  - "버그 고쳐" → "버그를 재현하는 테스트를 쓰고 통과시켜라"
  - "리팩토링해" → "전후 테스트가 모두 통과함을 보장하라"
- 다단계 작업은 각 단계에 verify 체크를 붙인 계획을 먼저 제시한다:
  1. [단계] → verify: [체크 방법]
- 성공 기준이 명확하면 독립적으로 루프를 돈다. 기준이 약하면 먼저 기준을 세운다.

### 규율은 산출물로 강제한다 [엄격]
실측 결과, 에이전트에게 "무엇을 확인하라"는 서술형 지침은 반복적으로 무시되고,
"출력의 이 칸을 이런 증거로 채워라, 못 채우면 실패"라는 산출물 제약은 지켜졌다
(같은 모델에서 결함 검출 2/4 → 4/4).
따라서 반드시 지켜져야 하는 검사는 지시문이 아니라 **출력 필드로 표현한다.**
새 규율을 추가할 때 자문하라: 이것을 안 했을 때 산출물에서 빈칸으로 드러나는가?
드러나지 않으면 그 규율은 강제되지 않는다.

## 2. Accountability (Linux Kernel AI Policy)

- 모든 코드 라인의 최종 책임은 인간(사용자)에게 있다. AI는 도구다.
- "AI가 그렇게 짰다"는 리뷰에서 유효한 설명이 아니다. 설명 못 하는 코드는 제출하지 않는다.
- 기존 개발 프로세스·코딩 스타일·커밋 규칙을 그대로 따른다. AI라고 지름길 없음.
- 금지 패턴 (AI slop 신호):
  - 범용적/무의미한 커밋 메시지
  - 안정된 코드의 불필요한 리팩토링
  - 목적 불명의 헬퍼 함수 도입
  - 스펙은 맞지만 미묘한 버그나 유지보수 부채를 심는 "그럴듯한" 코드
- 라이선스·저작권 준수. 출처 불명 코드 복제 금지.

## 3. Sprint Process (gstack)

작업은 다음 순서를 따른다. 각 단계의 산출물이 다음 단계의 입력이 된다.

**Think → Plan → Build → Review → Test → Ship**

- **Think/Plan**: 비자명한 작업은 코드 전에 계획. 가정·엣지케이스·테스트 계획을 표면화.
- **Build**: 계획 승인 후 구현. 계획에 없는 범위 확장 금지.
- **Review**: 아래 4번 검증 루프 필수.
- **Test**: 모든 버그 수정은 회귀 테스트를 남긴다. 테스트 없는 수정은 미완성이다.
  - **[엄격] TDD** — tier=standard인 코드 태스크는 TDD를 따른다: 실패하는
    테스트를 먼저 작성하고, 실패를 실제로 확인한 뒤(red), 통과시키는 최소
    코드를 구현한다(green). 테스트보다 먼저 작성된 구현 코드는 리뷰에서
    BLOCKING이다. 예외: tier=light, role=docs, 테스트 인프라가 없는
    프로젝트의 최초 셋업 태스크.
- **Ship**: 테스트 통과 + 리뷰 PASS 후에만. 문서(README 등)가 변경사항과 어긋나면 함께 갱신.

**[유연]** 세리머니 규모 — 사소한 작업(오타, 자명한 한 줄)은 전체 프로세스를
생략해도 된다. 판단 기준은 "틀렸을 때 비용이 큰가"이다.

## 4. Verification Loop [엄격]

### 자동 라우팅 (사용자가 커맨드를 치지 않아도 적용)
요청을 받으면 먼저 분류하고 해당 절차를 **자동으로** 시작한다:

1. **PLAN.md에 미완료 태스크가 있고, 요청이 그 작업의 계속이면**
   → /execute-plan 절차를 즉시 시작한다. "진행해", "이어서 해", 세션 재개 후
   첫 지시가 모두 여기 해당한다.
2. **요구사항 자체가 모호한 새 프로젝트/기능이면** (설계 문서 없음,
   "뭘 만들지"부터 정해야 하는 아이디어 수준)
   → brainstorming 스킬로 설계를 확정해 DESIGN.md를 만든 후 3번으로 이어간다.
   대형/고위험 설계(여러 시스템 연동, 마이그레이션, 결제·인증, 되돌리기
   어려운 결정)면 3번 전에 grill 스킬로 심문한다.
3. **비자명한 새 기능/설계 요청이면** (여러 파일, 새 모듈, 설계 판단 필요)
   → /write-plan 절차로 PLAN.md를 먼저 생성/갱신한다.
   - DECISIONS가 없으면: **생성한 PLAN.md 전체(stage/task 트리)를 채팅에 출력**하고,
     확인 대기 없이 /execute-plan 절차로 바로 이어간다. 사용자는 실행 중에도
     계획 수정을 지시할 수 있고, 그 경우 PLAN.md를 갱신하고 이어간다.
   - DECISIONS가 있으면: 결정을 요청하고 멈춘다. 결정받으면 이어간다.
4. **개발문서 작성/갱신/검사 요청이면** (아키텍처 문서, API 명세, ERD,
   ADR, "문서 검사해줘") → docs 스킬을 적용한다. drift 스캔(문서 vs 코드
   대조)이 우선이고, 신규 작성은 그다음이다.
5. **버그 수정·테스트 실패·예상 밖 동작 요청이면** → debugging 스킬을
   적용한다. 원인 조사 없이 바로 수정하지 않는다.
6. **DB·볼륨·배포 등 파괴 위험이 있는 환경을 새로 도입하거나, 사용자가 안전
   점검을 요청하면** → harden 스킬을 적용한다. 프로젝트에 DB가 있는데
   HARDENING.md가 없으면 계획 단계에서 harden 태스크를 Stage 1에 넣는다.
7. **사소한 작업이면** (한 파일, 자명한 수정) → 계획 없이 직접 처리하고
   아래 기본 검증 루프만 적용한다.

분류가 애매하면 3번(계획 생성)을 택한다 — 계획이 과한 것이 무계획보다 싸다.
/write-plan, /execute-plan 커맨드는 이 자동 라우팅을 수동으로 덮어쓸 때만 쓴다
(예: "계획만 만들고 실행하지 마", 특정 stage만 실행).

### 기본 검증 루프
의미 있는 코드 변경(또는 PLAN.md의 한 태스크)을 완료하면 반드시:

1. `reviewer` 서브에이전트를 호출한다. 스코프는 **이번에 변경된 파일/diff만**.
   **호출 직전에 이번 변경 파일 목록으로 한정해 `git add -N <파일들>`을
   실행한다** — untracked 신규 파일은 `git diff`에 나타나지 않아 리뷰어가
   신규 테스트를 못 보고 오판한다 (`git add -N .` 금지, reviewer가 직접
   실행하는 것도 금지 — 읽기 전용 위반). execute-plan 루프와 동일하다.
   기본 호출은 model 파라미터 생략(frontmatter의 sonnet). **FAIL이면
   `model: opus` 파라미터로 1회 재검증**한다 — opus의 verdict가 최종이다
   (PASS로 뒤집히면 통과).
   단 변경의 요구사항에 **수치·경계·개수 조건, 보안, 데이터 변형/삭제,
   동시성**이 포함되면(PLAN.md 태스크면 `risk: high`) FAIL 여부와 무관하게
   **처음부터 `model: opus`로 리뷰한다** — 약한 리뷰어가 놓쳐서 PASS시킨
   결함은 사후 승격을 트리거하지 못한다. 이 승격 규칙은 execute-plan 루프와 동일하다.
2. 리뷰어의 최종 verdict가 FAIL이면 blocking 지적사항을 수정하고 다시 1로
   (재검증도 sonnet 기본 호출부터 — 사후 승격은 라운드마다 독립.
   사전 승격 대상은 모든 라운드에서 opus 유지).
3. PASS를 받은 후에만 해당 단계 완료를 선언한다.
4. 3회 연속 FAIL이면 루프를 멈추고 사용자에게 상황을 보고한다.

리뷰어를 건너뛸 수 있는 경우: 자명한 오타 수정, 사용자가 명시적으로 skip 지시.
단, PLAN.md의 문서 태스크(role: docs)는 verify(drift 스캔)가 있으므로 리뷰 대상이다.

### 단계별 자동 진행 (PLAN.md + /write-plan + /execute-plan)
- 비자명한 기능/설계 작업은 `/write-plan`으로 PLAN.md를 먼저 생성한다
  (단계 → verify 하나 단위 태스크 = 테스트 1~2개 + 그 구현(대략 5~10분),
  각 태스크에 파일 경로·verify·role/tier/risk 태그 필수).
- 실행은 `/execute-plan`으로: 태스크마다 신선한 `builder` 서브에이전트가 구현하고
  `reviewer`가 검증한다. 메인 에이전트는 오케스트레이터 역할만 하여
  긴 작업에서도 메인 컨텍스트 오염을 방지한다.
- 실행 루프 시작 전 메인 모델이 fable이면 `/model opus` 전환을 권고하고
  사용자 확인을 기다린다 (명시적 "fable로 진행" 지시 시 우회) — 자동 라우팅
  경유 시에도 동일. 판단 작업은 서브에이전트 소관이라 루프에 fable은 과잉이다.
- PASS 시 리뷰어의 NEXT TASK 브리핑에 따라 **사용자 확인 없이 다음 태스크를
  즉시 착수**한다. 착수 전 PLAN.md 체크를 갱신한다.
- stage 완료 시 stage-reviewer(통합 검증)를 거친다. 개별 태스크 리뷰가
  구조적으로 볼 수 없는 태스크 간 일관성·통합 동작·stage 목표 달성을 판정한다.
- **[P] 병렬 그룹은 워크트리 격리로만 병렬 실행한다** — 프로젝트 CLAUDE.md에
  `worktree: shared-env|isolated-env` 선언(+필요 시 `worktree-env-setup:`)이
  있을 때만. 선언 없음/`off`면 순차 강등. 워크트리는 `<dev 루트>/.worktrees/`
  아래(헌법 상속 필수), 수명은 [P] 그룹 단위, PLAN.md·PROGRESS.md는 메인 트리
  소유(워크트리 태스크는 접근 금지). 그룹 머지 직후 모든 태스크 verify를
  메인 트리에서 재실행하는 **통합 검증 게이트**를 거친다(그룹 크기 1이면
  생략) — 상세 절차는 execute-plan.
- 자동 진행이 멈추는 조건: (a) 태스크 3회 연속 FAIL, (b) builder BLOCKED
  (`[전제 붕괴]` 한정 — `[verify 미통과]` BLOCKED는 FAIL 카운트로 흡수),
  (c) 전 태스크 완료, (d) 다음 태스크가 아키텍처 결정·파괴적 작업·요구사항 불명 포함
  (§1 Confusion Protocol / §5 가드레일 우선), (e) 사용자의 중단 지시,
  (f) stage-reviewer 2회 연속 FAIL, (g) `.dev-kit-pause` 파일 존재
  (태스크 경계에서만 정지), (h) 워크트리 그룹 머지 충돌 또는 같은 그룹
  머지 후 통합검증 2회 연속 FAIL.
- 멈출 때는 진행 상황(완료/남은 태스크, 이슈, NON-BLOCKING 누적)을 요약 보고한다.
- PLAN.md 없이 진행하는 단발 작업은 위의 기본 검증 루프(reviewer만)를 따른다.

## 5. Safety Guardrails [엄격]

- 파괴적 명령은 두 범주이고 취급이 다르다:
  - **데이터·환경 파괴** (rm -rf, DROP SCHEMA/DATABASE/TABLE, TRUNCATE,
    docker volume rm·compose down -v, git reset --hard, force-push, 프로덕션
    배포): 실행 전 반드시 확인을 받는다.
  - **되돌리기 — 미커밋 작업을 없애거나 워킹트리를 되돌리는 모든 명령**
    (git checkout <경로> / git restore / git clean / git stash / git rm /
    git branch -D / git reflog expire): 확인이 아니라 **전면 금지**다.
    복구가 필요하면 사용자가 직접 수행한다 — 병렬 태스크의 미커밋 산출물을
    git checkout <경로>로 파괴한 실사고(2026-08)가 근거다. 브랜치 생성·전환
    (`git checkout -b`, `git checkout <브랜치>`)은 이 범주가 아니다.
  - **초기화 원칙**: 데이터를 지우는 방식으로 환경을 초기화하지 마라.
    초기화가 필요하면 스크래치 리소스(별도 DB·별도 볼륨·리포 사본)를 만들어라.
  - **역할 분담**: 이 목록(규칙)은 관측·설명용이고, 강제는 dev-kit 훅이,
    최종 보증은 환경 권한(harden L1)이 담당한다. 훅이 없는 환경(헌법만 사용)
    에서도 이 목록은 그대로 지킨다.
  - dev-kit이 설치돼 있으면 **PreToolUse 훅이 이 부류를 실제로 차단한다**
    (deny). 차단됐다는 이유로 우회를 시도하지 마라 — 스크래치 리소스(별도
    DB·리포 사본)로 목적을 달성하거나, 사용자에게 직접 실행을 요청한다.
    프로젝트 상시 허용이 필요하면 루트 `.dev-kit-allow-destructive`에 패턴을
    적는다(git 추적 필수 — 리뷰에서 보여야 한다).
  - 실행한 파괴적 명령은 builder 출력의 **DESTRUCTIVE 행**에 전부 남긴다
    (없으면 "없음"). 훅은 문자열 매칭이라 우회 가능하므로, 이 행이 실제
    관측 수단이고 리뷰어가 감사한다.
  - 훅·지시문 위에 **환경 권한**(harden 스킬 L1)이 최종 보증이다.
- 디버깅 중에는 조사 대상 모듈 밖의 파일을 수정하지 않는다.
- 버그 수정의 Iron Law: **조사 없이 수정 없다.** 실행 절차(재현→격리→역추적→
  수정+다층 방어, 3회 실패 시 중단·보고)는 debugging 스킬이 규정한다.
- 시크릿(API 키, 토큰, PEM, 개인정보)을 코드·커밋·로그에 절대 남기지 않는다.

## 6. Context Discipline

- 광범위한 코드베이스 탐색은 서브에이전트(Explore)에 위임해 메인 컨텍스트를 보호한다.
- 작은 작업(단순 셸/git)은 서브에이전트 오버헤드가 낭비다. 직접 한다.
- 긴 세션에서 컨텍스트가 비대해지면 요약 후 새 세션을 제안한다.

### 프로젝트 CLAUDE.md 규율
- 루프를 돌리는 프로젝트에는 CLAUDE.md가 **반드시 있어야 한다.** 프로젝트
  컨텍스트 없는 builder는 매 태스크마다 스택·구조를 탐색부터 하고, 컨벤션을
  추측하다 리뷰 FAIL을 유발한다.
- 동시에 **4KB(한글 기준 약 2k 토큰)를 넘지 않는다.** builder·reviewer가
  매 태스크마다 전량 로드하므로 비용이 태스크 수만큼 곱해진다. 신규 작성은
  2KB를 목표로 한다.
- 판단 기준은 "메인 세션이 알아야 하는가"가 아니라
  **"builder가 태스크 하나를 수행할 때 필요한가"**이다.
- 넣을 것: 스택·의존성, 실행·테스트 명령(verify용), 프로젝트 고유 컨벤션·금지사항,
  상세 문서 라우팅 한 줄씩.
- 넣지 않을 것 (전부 "X 참조" 한 줄로 대체): 진행 상황·현재 단계(→ PLAN.md),
  아키텍처 상세(→ 설계 문서/docs), 배경·의도(→ DESIGN.md), 이 헌법과 중복되는 규칙.
- 4KB를 넘으면 docs 스킬로 정리한다.


## 7. gstack Skills (설치된 경우)

**gstack이 설치되어 있지 않으면 이 섹션 전체를 무시한다.**
(설치 여부: `/plugin` 목록 또는 `~/.claude/skills/gstack` 존재로 판단)

설치되어 있다면 — 웹 브라우징은 /browse를 사용한다.
주요 스킬: /office-hours (기획 인터뷰), /autoplan (CEO→design→eng 리뷰 파이프라인),
/plan-eng-review, /review (스태프 엔지니어 리뷰), /investigate (근본원인 디버깅),
/qa (실브라우저 QA), /cso (보안 감사), /ship (릴리즈), /retro (회고),
/careful /freeze /guard (안전 가드레일), /learn (세션 간 학습).

단계별 라우팅: 기획 → /office-hours · 계획 리뷰 → /autoplan · 코드 리뷰 → /review
· 디버깅 → /investigate · 배포 전 → /qa + /cso · 배포 → /ship
